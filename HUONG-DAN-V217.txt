V217 - LEGACY SALE UUID FINALIZER

Lỗi đã xác định chính xác:
2 phiếu bán legacy, mỗi phiếu 5 movement.
sale.id không phải UUID Cloud hợp lệ.

V217 xử lý migration-safe:
- Tạo UUID deterministic từ legacy sale id bằng cùng namespace references của V198.
- Upsert sales header với UUID mới.
- Upsert sale_items với UUID deterministic.
- Upsert 10 SALE stock_transactions với UUID deterministic và reference_id mới.
- KHÔNG gọi commit_sale_receipt RPC ở bước UUID hóa.
- KHÔNG thay đổi inventory.
- Chỉ remap local sau khi Cloud verify đủ movement.
- Lưu permanent map old sale id -> Cloud UUID để cache/thiết bị không phục hồi ID cũ.
- V205 normalize được mở rộng cho sales + saleItems.

Không cần SQL mới.

Sau update:
Cài đặt > Công cụ đồng bộ nâng cao > Chạy kiểm tra.

Kỳ vọng:
Pending Retry: "UUID hóa 2 phiếu legacy"
Pending = 0: PASS
