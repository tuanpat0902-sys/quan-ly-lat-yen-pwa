V209 - LOOP GUARD

Nguyên nhân vòng lặp V208:
cacheSave() vừa được dùng để lưu cache vừa làm trigger sync.
Nhưng sync nội bộ cũng gọi cacheSave(), nên có thể tự xếp thêm lượt sync.

V209:
- cacheSave() chỉ lưu cache, KHÔNG kích sync.
- Chỉ thao tác nghiệp vụ người dùng mới kích sync.
- Action sync có internal guard.
- Storage hook bị khóa khi autoSync/syncNow/realtime đang chạy.
- Cooldown action sync: 1.2 giây.
- Khi bận/lỗi: tối đa 1 follow-up, không retry vô hạn.
- Realtime V207 vẫn hoạt động.
- Safe Merge V204/V205 vẫn giữ nguyên.

Đã gắn action trigger:
saveIngredient, deleteIngredient, saveRecipe, deleteRecipe, saveImportReceipt, deleteImportReceipt, saveExportReceipt, deleteExportReceipt, saveStocktakeReceipt, saveEditedStocktakeReceipt, deleteStocktakeReceipt, saveSaleReceipt, editSaleReceipt, deleteSaleReceipt, addCashflowEntry, deleteCashflowEntry, saveEmployee, deleteEmployee, saveAttendanceForEmployee, saveWarehouse, deleteWarehouse

Không cần SQL mới.
