(()=>{'use strict';if(window.__lyLegacyHelperShimV2)return;window.__lyLegacyHelperShimV2=true;const VERSION='2026.08.24.2';const installed=[];const def=(name,fn)=>{if(typeof window[name]==='undefined'){window[name]=fn;installed.push(name)}};const esc=s=>String(s??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));const num=v=>{const n=Number(String(v??'').replace(/[,_\s₫đ]/gi,''));return Number.isFinite(n)?n:0};const intFmt=v=>new Intl.NumberFormat('vi-VN',{maximumFractionDigits:0}).format(num(v));const money=v=>`${intFmt(v)} đ`;def('esc',esc);def('escapeHtml',esc);def('htmlEscape',esc);def('safeHtml',esc);def('safeText',v=>v==null?'':String(v));def('asArray',v=>Array.isArray(v)?v:(v==null?[]:[v]));def('asNumber',num);def('toNumber',num);def('parseMoney',num);def('fmtNumber',intFmt);def('formatNumber',intFmt);def('money',money);def('fmtMoney',money);def('formatMoney',money);def('moneyText',money);def('formatCurrency',money);def('currencyText',money);def('clamp',(v,min,max)=>Math.min(Number(max),Math.max(Number(min),Number(v))));def('noop',()=>{});def('uid',prefix=>`${prefix||'id'}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`);def('coalesce',(...values)=>values.find(v=>v!==undefined&&v!==null));def('isBlank',v=>v==null||String(v).trim()==='');def('nonEmpty',v=>!window.isBlank(v));window.__lyLegacyHelperShim={version:VERSION,installed:[...installed],esc,money,status:()=>({version:VERSION,installed:[...installed]})};window.dispatchEvent?.(new CustomEvent('latyen:legacy-helper-shim-ready',{detail:{version:VERSION,installed:[...installed]}}));})();