V196 - DIAGNOSTICS FIX

Sửa hai lỗi từ ảnh V195:
1. RPC bị báo thiếu giả do diagnostics gọi RPC với {} không đúng chữ ký.
   V196 probe bằng đúng tên tham số.
2. Pending bị đếm trùng:
   imports/exports/stocktakes là subtype của movements.
   V196 hiển thị unique_total riêng.

V196 còn phân tích pending legacy:
- reference_id không phải UUID
- ingredient_id local/non-UUID
- stocktake note cũ thiếu "Thực tế"
Các record này được báo riêng vì không thể retry atomic tự động.

Không cần SQL mới nếu SQL Full Update V192 đã chạy.
