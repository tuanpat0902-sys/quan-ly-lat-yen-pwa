V211 - SALE PENDING RECONCILE

Triệu chứng:
Migration 100%, nhưng Pending Inspector còn Movement bán và không có lỗi cụ thể.

Nguyên nhân:
syncPendingSaleReceipts() chỉ retry khi phiếu sale itself còn pending.
Nếu sale đã commit thành công nhưng movement local vẫn giữ _sync_status=pending,
các movement này không được xử lý.

V211:
- Gom pending SALE movement theo reference_id.
- Query sales + stock_transactions trên Cloud.
- Chỉ reconcile khi Cloud đã có sale và đầy đủ movement khớp:
  reference_id + warehouse_id + ingredient_id + quantity.
- Xóa movement pending local stale.
- Chèn movement Cloud authoritative.
- Clear stale pending flags của sale/saleItems cùng receipt.
- Không gọi lại RPC bán hàng khi Cloud đã có dữ liệu -> không trừ tồn kho lần 2.
- Chạy reconcile trước và sau retry pending.

Không cần SQL mới.

Sau update:
1. Mở Cài đặt > Công cụ đồng bộ nâng cao.
2. Bấm "Thử đồng bộ lại" 1 lần.
3. 10 Movement bán pending cũ sẽ tự biến mất nếu Cloud đã có đủ dữ liệu.
