import assert from 'node:assert/strict';
import vm from 'node:vm';
import fs from 'node:fs/promises';

const source=await fs.readFile(new URL('../ly-fresh-core-v2-products-takeover.js',import.meta.url),'utf8');
const previousCalls=[];const productSaveCalls=[];let loadCalls=0;let renders=0;
let snapshot={products:[{id:'p1',name:'P'}],recipeItems:[{product_id:'p1',ingredient_id:'i1',quantity:2}]};
const client={async rpc(name,params){previousCalls.push([name,params]);return {data:`previous:${name}`,error:null};}};
const priorRpc=client.rpc.bind(client);client.rpc=async function(name,params){if(name==='ly_save_ingredient'){previousCalls.push(['ingredients-wrapper',params]);return {data:'ingredient-v2-id',error:null};}return priorRpc(name,params);};
const context={console,setTimeout(fn){fn();return 1;},clearTimeout(){},CustomEvent:class CustomEvent{constructor(type,init={}){this.type=type;this.detail=init.detail;}},document:{readyState:'complete',addEventListener(){}},window:{sb:client,__lyFreshCoreV2:{store:{getState(){return snapshot;}},domains:{products:{async save(product,recipeItems){productSaveCalls.push([product,recipeItems]);snapshot={products:[{...product,id:'product-v2-id'}],recipeItems};return 'product-v2-id';}}}},dispatchEvent(){}}};
context.db={products:[],recipeItems:[]};context.invalidateDerivedCaches=()=>{};context.renderRecipes=()=>{renders++;};context.renderSales=()=>{};context.renderDashboard=()=>{};context.loadCloud=async()=>{loadCalls++;return {legacy:true};};context.globalThis=context;
vm.createContext(context);vm.runInContext(source,context,{filename:'ly-fresh-core-v2-products-takeover.js'});
assert.equal(context.window.__lyFreshCoreV2ProductsTakeover.status().enabled,true);
const product={name:'P'};const recipe=[{ingredient_id:'i1',quantity:2}];
const result=await client.rpc('ly_save_product',{p_product:product,p_recipe_items:recipe});
assert.equal(result.data,'product-v2-id');assert.equal(previousCalls.length,0);assert.deepEqual(productSaveCalls,[[product,recipe]]);
assert.equal(context.db.products[0].id,'product-v2-id');assert.deepEqual(context.db.recipeItems,recipe);assert.ok(renders>0);
await context.loadCloud();assert.equal(loadCalls,0,'first post-save loadCloud must be suppressed');
await context.loadCloud();assert.equal(loadCalls,1,'later/manual loadCloud must remain available');
const ingredient=await client.rpc('ly_save_ingredient',{p_ingredient:{name:'A'},p_prepared_items:[]});assert.equal(ingredient.data,'ingredient-v2-id');
await context.loadCloud();assert.equal(loadCalls,2,'non-product RPC must not arm suppression');
context.window.__lyFreshCoreV2ProductsTakeover.disable();await client.rpc('ly_save_product',{p_product:{name:'Legacy'},p_recipe_items:[]});assert.equal(previousCalls.at(-1)[0],'ly_save_product');
await context.loadCloud();assert.equal(loadCalls,3,'disable must restore original loadCloud');
console.log('Fresh Core V2 products takeover: PASS');