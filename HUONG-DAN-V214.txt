V214 - MOVEMENT CLOUD ACK

Nguyên nhân 10 SALE movement vẫn pending:
- V212/V213 yêu cầu Cloud phải có sale header tương ứng.
- Nhưng migration cũ migrate stock_transactions, không migrate toàn bộ sales/sale_items.
- Vì vậy movement có thể đã ở Cloud nhưng sale header cũ không có.

V214:
- Đối chiếu trực tiếp pending SALE movement với public.stock_transactions.
- Không phụ thuộc bảng sales.
- Ưu tiên match cùng movement UUID.
- Nếu UUID khác do migration, fallback theo:
  reference_id + warehouse_id + ingredient_id + quantity.
- Chỉ xóa đúng local pending row đã được chứng minh tồn tại trên Cloud.
- Không gọi RPC sale.
- Không update inventory.
- Không clear sale/sale_items.
- Không trừ tồn kho lần 2.

Không cần SQL mới.

Sau update:
Cài đặt > Công cụ đồng bộ nâng cao > Chạy kiểm tra
hoặc Thử đồng bộ lại.

Nếu 10 movement đúng là đã nằm trên Cloud, Pending = 0 sẽ PASS.
Nếu movement nào thật sự chưa có Cloud, nó vẫn được giữ pending.
