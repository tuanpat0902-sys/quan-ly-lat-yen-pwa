{
  "name": "quan-ly-lat-yen-pwa",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "stability": "node scripts/stability-audit.mjs",
    "check": "npm run validate && npm run stability"
  },
  "engines": {
    "node": ">=20"
  }
}
