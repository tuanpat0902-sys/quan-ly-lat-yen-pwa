{
  "name": "quan-ly-lat-yen-pwa",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "stability": "node scripts/stability-audit.mjs",
    "perf:budget": "node scripts/performance-budget.mjs",
    "check": "npm run validate && npm run stability && npm run perf:budget"
  },
  "engines": {
    "node": ">=20"
  }
}
