V191 - FIX FALSE SYNC + ATOMIC IMPORT

Lỗi tìm thấy:
- Phiếu nhập bị _sync_status=pending nhưng syncNow/Auto Sync không retry.
- Trạng thái "Đã đồng bộ" chỉ đếm queue ingredient/recipe + sales, bỏ qua inventory/movement/supplier/import.
- Vì vậy Nhật ký có thể hiện "Chờ đồng bộ Cloud" nhưng header vẫn báo đồng bộ.

V191 sửa:
1. Đếm pending thật ở inventory, movement, supplier, ingredient, sales, sale_items.
2. Auto Sync retry supplier pending.
3. Auto Sync retry toàn bộ IMPORT movement pending theo reference_id.
4. Phiếu nhập mới được stage local toàn bộ rồi commit bằng 1 RPC atomic.
5. Inventory nhập kho chỉ thay đổi bằng delta trong PostgreSQL transaction.
6. Không còn ghi inventory quantity tuyệt đối từ luồng nhập kho.
7. Chỉ báo "Đã đồng bộ hoàn toàn" khi pending thật = 0.

CÀI ĐẶT:
- Giữ SQL V189 + V190 đã chạy.
- Chạy thêm SUPABASE-V191-ATOMIC-IMPORT.sql.
- Deploy V191.
- Trên thiết bị đang có phiếu "Chờ đồng bộ Cloud", bấm Đồng bộ ngay.
