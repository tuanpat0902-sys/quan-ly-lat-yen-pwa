V230 - SALE PAIR NORMALIZER

Tình trạng V229:
- Chỉ còn 2 record chưa được Cloud xác nhận:
  + sales: 1
  + sale_items: 1
- pending: 0
- movements: 0 missing

Nguyên nhân còn lại:
V229 có thể đã hoàn thiện lịch sử trên Cloud nhưng chưa chuẩn hóa vật lý
sale.id / sale_item.id trong local/canonical, nên cặp ID cũ bị tái xuất hiện.

V230:
1. Tìm UUID phiếu Cloud theo bằng chứng mạnh nhất:
   - reference_id của SALE movement đã tồn tại trên Cloud;
   - legacy map V205;
   - hoặc kho + tổng tiền + thời gian + món + SL + đơn giá.
2. Nếu Cloud đã có phiếu:
   - remap local sale.id về UUID Cloud.
   - remap sale_item.sale_id và sale_item.id về row Cloud thật.
3. Nếu Cloud chưa có header/item:
   - chỉ upsert sales + sale_items lịch sử.
   - KHÔNG gọi RPC bán hàng.
4. Verify Cloud trước khi thay local.
5. Sau verify:
   - chuẩn hóa vật lý local IDs.
   - cập nhật movement.reference_id local nếu còn dùng ID cũ.
   - deduplicate local.
   - lưu V205 reference map.
6. Canonical sync chạy SAU bước chuẩn hóa nên ID cũ không bị phục hồi nữa.

An toàn:
- Không ghi inventory.
- Không cộng/trừ tồn.
- Không gọi commit_sale_receipt_v190.
- Không sửa 144 movement Cloud đã sạch.
- Không xóa dữ liệu trước verify.
- Không cần SQL mới.

Sau deploy:
Cài đặt > Đồng bộ ngay.
Kỳ vọng: 100% và Cloud xác nhận toàn bộ record.
