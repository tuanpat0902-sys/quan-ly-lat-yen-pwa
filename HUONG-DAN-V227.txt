V227 - CLOUD COVERAGE FIX

Lỗi 92% đã xác định:
V226 coi reference_id của tất cả stock_transactions bắt buộc phải là UUID.

Quy tắc đúng:
- SALE: reference_id liên kết sales.id => phải là UUID.
- IMPORT: mã phiếu có thể là text.
- EXPORT: mã phiếu có thể là text.
- ADJUSTMENT/KIỂM KÊ: session/mã phiếu có thể là text.

V227:
- Không gắn nhầm Nhập/Xuất/Kiểm kê là local-only.
- Kiểm tra đầy đủ ID bắt buộc của các bảng Cloud.
- SALE vẫn kiểm tra UUID nghiêm ngặt.
- V205 legacy ghost vẫn bị chặn.
- Không xóa record.
- Không thay đổi inventory.
- Không tự chạy migration.
- Không cần SQL mới.
- Nếu còn lỗi thật, Settings sẽ hiện breakdown theo từng nhóm.

Sau cập nhật:
Cài đặt > Đồng bộ ngay một lần.
Nếu dữ liệu đã sạch, tiến độ sẽ về 100%.
