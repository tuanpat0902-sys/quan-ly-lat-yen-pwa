V252 - ROOT FIX WAREHOUSE + PENDING

1) Kho mới không còn nhận công thức cũ:
- Trước khi tạo kho mới, mọi product legacy chưa có ownership được khóa về kho cũ đang dùng.
- ensureProductWarehouseOwnership không bao giờ dùng currentWarehouseId làm fallback nữa.
- Scope được suy từ lịch sử bán; nếu không có lịch sử thì dùng kho cũ/oldest ổn định.
- Mapping cũ kiểu auto-claim vào một kho mới, trống, tạo sau product sẽ tự được sửa.
- Product mới tạo trong kho mới vẫn được saveRecipe assign explicit vào kho mới.

2) 492/492 nhưng còn pending:
- SyncNow và Auto Sync fetch Cloud thật rồi exact-ACK các row pending.
- Chỉ clear pending khi giá trị Cloud thực sự khớp local.
- save_recipe queue chỉ ACK khi product + toàn bộ recipe lines khớp Cloud.
- save_ingredient queue chỉ ACK khi ingredient (+ prepared lines) khớp Cloud.
- Reset Cloud không được báo thành công nếu vẫn còn pending thật.

Không cần SQL mới.
Giữ nguyên V251/V250/V248/V246/V245/V244/V242.

Hiệu năng Exact ACK:
- Tận dụng chính snapshot Cloud vừa Pull, không gọi lại 10 bảng lần nữa.
- Chỉ chạy Exact ACK khi Cloud Coverage đã 100% nhưng pending vẫn > 0.
