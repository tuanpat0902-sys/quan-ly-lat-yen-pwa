V197 - SYNC REPAIR

Đã xác định từ ảnh:
- RPC tồn tại nhưng digest() không resolve vì pgcrypto ở schema extensions.
- 170 pending movement là Excel/local non-UUID.
- 5 inventory pending cũng là Excel/local.
- 2 sales pending còn lại cần retry sau khi sửa RPC.

BƯỚC 1:
Chạy SUPABASE-V197-PGCRYPTO-HOTFIX.sql trong Supabase SQL Editor.

BƯỚC 2:
Deploy V197.

BƯỚC 3:
Cài đặt -> Đồng bộ -> bấm "Sửa pending local".
V197 sẽ:
- gỡ pending/error khỏi inventory Excel/local
- gỡ pending/error khỏi movement Excel/local
- giữ chúng _local_only=true
- không gửi ID excel_* vào cột UUID nữa
- chạy syncNow ngay sau đó

Kỳ vọng:
- 162 kiểm kê local + 7 nhập local + 1 xuất local + 5 inventory local biến mất khỏi pending.
- 2 sales được retry bằng RPC sau khi pgcrypto fix.
- Pending tiến về 0 nếu không còn lỗi khác.
