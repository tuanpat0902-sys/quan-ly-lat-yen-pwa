V259 - QR + SYNC SIMULATION FIX

1. QR khi thiết bị đã ngắt kết nối
- BarcodeDetector nếu có: dùng đường nhanh.
- Nếu không có: tự tải jsQR 1.4.0 và giải mã camera bằng canvas.
- Scanner hoạt động ngay trên màn hình DISCONNECTED, không phụ thuộc init chính.
- Nếu camera/bộ giải mã vẫn không mở: dùng Dán QR hoặc nhập code + passphrase.

2. Mô phỏng đồng bộ
- Bỏ phép đếm raw local rows cũ gây 383/591 = 66.5%.
- Dùng cùng v228CaptureCloudProof + v228Coverage với sync thật.
- Nếu recipe coverage cần repair thì gọi V258 trước.
- Local → Cloud hiển thị covered/total theo authoritative Cloud Proof.
- Device A → Canonical → B vẫn chỉ merge trong RAM, không commit.

Không cần SQL mới.
Giữ nguyên V258/V257/V256/V255/V254/V253/V252/V251/V250.
